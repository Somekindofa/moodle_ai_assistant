function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
define(["exports", "core/ajax", "local_craftpilot/marked", "local_craftpilot/dompurify"], function (_exports, Ajax, MarkedLib, _dompurify) {
  "use strict";

  Object.defineProperty(_exports, "__esModule", {
    value: true
  });
  _exports.init = void 0;
  Ajax = _interopRequireWildcard(Ajax);
  MarkedLib = _interopRequireWildcard(MarkedLib);
  _dompurify = _interopRequireDefault(_dompurify);
  function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
  function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function _interopRequireWildcard(e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != _typeof(e) && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (var _t3 in e) "default" !== _t3 && {}.hasOwnProperty.call(e, _t3) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, _t3)) && (i.get || i.set) ? o(f, _t3, i) : f[_t3] = e[_t3]); return f; })(e, t); }
  function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t.return || t.return(); } finally { if (u) throw o; } } }; }
  function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
  function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
  function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
  function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
  function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
  function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
  function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
  function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
  function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
  function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
  function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
  function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
  function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
  function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
  function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
  function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
  function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
  function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
  function _arrayWithHoles(r) { if (Array.isArray(r)) return r; } /**
 * CraftPilot Chat Interface — site-wide widget (local_craftpilot)
 *
 * Adapted from mod_craftpilot: removed cmId / instanceId, widget is
 * injected on every authenticated Moodle page via page_init / before_footer.
 *
 * @module     local_craftpilot/chat_interface
 * @copyright  2026
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
  /* ============================================================
     STATE
     ============================================================ */
  var state = {
    courseId: null,
    // retrieval hint; 0 = no course context
    chatProxyUrl: null,
    // set from PHP via init()
    currentConvId: null,
    currentConvTitle: 'CraftPilot',
    conversations: [],
    sidebarOpen: false,
    chatOpen: false,
    streaming: false,
    streamAbort: null,
    // AbortController for the active fetch; null when idle
    sources: [],
    selectedDomain: null,
    followUpQuestions: {
      A: null,
      B: null,
      C: null
    },
    isNewConv: false // true after createConversation, reset on first send
  };

  /* Per-conversation state — preserves sources across conv switches */
  var convStates = {};
  var getConvState = function getConvState(id) {
    if (!id) return {
      sources: []
    };
    var key = String(id);
    if (!convStates[key]) convStates[key] = {
      sources: []
    };
    return convStates[key];
  };

  /* DOM element references — populated in initDOM() */
  var dom = {};

  /* ============================================================
     PUBLIC INIT (AMD entry point)
     ============================================================ */
  var init = _exports.init = function init(moduleCourseId, chatProxyUrl) {
    state.courseId = moduleCourseId || 0;
    state.chatProxyUrl = chatProxyUrl || '/local/craftpilot/chat_proxy.php';
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', setup);
    } else {
      setup();
    }
  };

  /* ============================================================
     SETUP
     ============================================================ */
  var setup = function setup() {
    if (!initDOM()) return;
    bindEvents();
    loadConversations();
  };
  var initDOM = function initDOM() {
    var map = {
      toggle: 'cp-toggle',
      wrapper: 'cp-wrapper',
      chatBody: 'cp-chat-body',
      sidebar: 'cp-sidebar',
      sidebarToggle: 'cp-sidebar-toggle',
      newConv: 'cp-new-conv',
      convList: 'cp-conv-list',
      panel: 'cp-panel',
      convTitle: 'cp-conv-title',
      closeBtn: 'cp-close',
      messages: 'cp-messages',
      sources: 'cp-sources',
      sourcesHeader: 'cp-sources-header',
      sourcesCount: 'cp-sources-count',
      sourcesScroll: 'cp-sources-scroll',
      domainBar: 'cp-domain-bar',
      input: 'cp-input',
      sendBtn: 'cp-send'
    };
    var ok = true;
    var required = new Set(['toggle', 'wrapper', 'messages', 'input', 'sendBtn']);
    Object.entries(map).forEach(function (_ref) {
      var _ref2 = _slicedToArray(_ref, 2),
        key = _ref2[0],
        id = _ref2[1];
      dom[key] = document.getElementById(id);
      if (!dom[key] && required.has(key)) {
        console.error('CraftPilot: missing required element #' + id);
        ok = false;
      }
    });
    return ok;
  };

  /* ============================================================
     EVENT BINDING
     ============================================================ */
  var bindEvents = function bindEvents() {
    var _dom$closeBtn, _dom$sidebarToggle, _dom$newConv, _dom$sourcesHeader, _dom$domainBar;
    dom.toggle.addEventListener('click', toggleChat);
    dom.toggle.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        toggleChat();
      }
    });
    (_dom$closeBtn = dom.closeBtn) === null || _dom$closeBtn === void 0 || _dom$closeBtn.addEventListener('click', function () {
      return setChat(false);
    });
    (_dom$sidebarToggle = dom.sidebarToggle) === null || _dom$sidebarToggle === void 0 || _dom$sidebarToggle.addEventListener('click', toggleSidebar);
    (_dom$newConv = dom.newConv) === null || _dom$newConv === void 0 || _dom$newConv.addEventListener('click', startNewConversation);
    dom.sendBtn.addEventListener('click', sendMessage);
    dom.input.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();
        sendMessage();
      }
    });
    dom.input.addEventListener('input', autoResize);
    (_dom$sourcesHeader = dom.sourcesHeader) === null || _dom$sourcesHeader === void 0 || _dom$sourcesHeader.addEventListener('click', toggleSources);
    (_dom$domainBar = dom.domainBar) === null || _dom$domainBar === void 0 || _dom$domainBar.addEventListener('click', function (e) {
      var btn = e.target.closest('.cp-domain-btn');
      if (!btn) return;
      var domain = btn.dataset.domain;
      if (state.selectedDomain === domain) {
        state.selectedDomain = null;
        btn.classList.remove('cp-domain-btn--active');
      } else {
        state.selectedDomain = domain;
        dom.domainBar.querySelectorAll('.cp-domain-btn').forEach(function (b) {
          return b.classList.remove('cp-domain-btn--active');
        });
        btn.classList.add('cp-domain-btn--active');
      }
    });

    // Abort any in-flight stream when the user navigates away or refreshes,
    // so the browser is not blocked waiting for the fetch to settle.
    window.addEventListener('beforeunload', function () {
      if (state.streamAbort) state.streamAbort.abort();
    });
  };

  /* ============================================================
     CHAT OPEN / CLOSE
     ============================================================ */
  var setChat = function setChat(open) {
    var _dom$chatBody;
    state.chatOpen = open;
    dom.wrapper.classList.toggle('cp-wrapper--open', open);
    (_dom$chatBody = dom.chatBody) === null || _dom$chatBody === void 0 || _dom$chatBody.setAttribute('aria-hidden', String(!open));
    dom.toggle.setAttribute('aria-expanded', String(open));
    dom.toggle.classList.toggle('cp-toggle-pill--active', open);
    if (open) setTimeout(function () {
      var _dom$input;
      return (_dom$input = dom.input) === null || _dom$input === void 0 ? void 0 : _dom$input.focus();
    }, 50);
  };
  var toggleChat = function toggleChat() {
    return setChat(!state.chatOpen);
  };

  /* ============================================================
     SIDEBAR
     ============================================================ */
  var toggleSidebar = function toggleSidebar() {
    var _dom$sidebar, _dom$wrapper;
    state.sidebarOpen = !state.sidebarOpen;
    (_dom$sidebar = dom.sidebar) === null || _dom$sidebar === void 0 || _dom$sidebar.classList.toggle('cp-sidebar--open', state.sidebarOpen);
    (_dom$wrapper = dom.wrapper) === null || _dom$wrapper === void 0 || _dom$wrapper.classList.toggle('cp-wrapper--sidebar-open', state.sidebarOpen);
  };

  /* ============================================================
     SOURCES SECTION
     ============================================================ */
  var toggleSources = function toggleSources() {
    var _dom$sourcesHeader2;
    var open = dom.sources.classList.toggle('cp-sources--open');
    (_dom$sourcesHeader2 = dom.sourcesHeader) === null || _dom$sourcesHeader2 === void 0 || _dom$sourcesHeader2.setAttribute('aria-expanded', String(open));
  };
  var setSources = function setSources(items) {
    var _dom$sourcesHeader3;
    state.sources = items;
    if (!dom.sources || !dom.sourcesScroll) return;
    if (!items.length) {
      dom.sources.classList.remove('cp-sources--visible', 'cp-sources--open');
      dom.sourcesScroll.innerHTML = '';
      return;
    }
    dom.sources.classList.add('cp-sources--visible', 'cp-sources--open');
    (_dom$sourcesHeader3 = dom.sourcesHeader) === null || _dom$sourcesHeader3 === void 0 || _dom$sourcesHeader3.setAttribute('aria-expanded', 'true');
    if (dom.sourcesCount) dom.sourcesCount.textContent = items.length;
    dom.sourcesScroll.innerHTML = '';
    items.forEach(function (item) {
      return dom.sourcesScroll.appendChild(buildSourceCard(item));
    });
  };
  var addSource = function addSource(item, ownerConvId) {
    var targetConvId = ownerConvId || state.currentConvId;
    var convSrcs = getConvState(targetConvId).sources;
    if (convSrcs.some(function (s) {
      return s.id === item.id;
    })) return;
    convSrcs.push(item);
    // Only update the visible DOM if this source belongs to the active conversation
    if (String(targetConvId) === String(state.currentConvId)) {
      state.sources = convSrcs;
      setSources(state.sources);
    }
  };
  var clearSources = function clearSources() {
    return setSources([]);
  };

  /* ============================================================
     SOURCE CARDS
     ============================================================ */
  var buildSourceCard = function buildSourceCard(item) {
    var card = document.createElement('div');
    card.className = 'cp-source-card cp-card--' + item.type;
    card.setAttribute('role', 'listitem');
    card.setAttribute('tabindex', '0');
    card.setAttribute('aria-label', (item.filename || item.type) + ' — click to open');
    if (item.type === 'video') {
      card.innerHTML = '<div class="cp-card-thumb">' + '<div class="cp-card-play">' + '<svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor">' + '<path d="M8 5v14l11-7z"/>' + '</svg>' + '</div>' + '</div>' + '<div class="cp-card-footer">' + '<span class="cp-card-badge">Video</span>' + '<span class="cp-card-name">' + escapeHtml(item.filename || 'Video clip') + '</span>' + '</div>';
      card.addEventListener('click', function () {
        return openVideoModal(item);
      });
    } else if (item.type === 'bvh') {
      card.innerHTML = '<div class="cp-card-thumb">' + '<svg width="34" height="34" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round">' + '<circle cx="12" cy="3.5" r="1.5"/>' + '<line x1="12" y1="5"  x2="12" y2="11"/>' + '<line x1="12" y1="8"  x2="8"  y2="6"/>' + '<line x1="12" y1="8"  x2="16" y2="6"/>' + '<line x1="12" y1="11" x2="10" y2="17"/>' + '<line x1="12" y1="11" x2="14" y2="17"/>' + '<line x1="10" y1="17" x2="9.5" y2="21"/>' + '<line x1="14" y1="17" x2="14.5" y2="21"/>' + '</svg>' + '</div>' + '<div class="cp-card-footer">' + '<span class="cp-card-badge">Motion</span>' + '<span class="cp-card-name">' + escapeHtml(item.filename || 'Motion data') + '</span>' + '</div>';
      card.addEventListener('click', function () {
        return openBVHModal(item);
      });
    } else if (item.type === 'text') {
      var preview = escapeHtml((item.content || '').substring(0, 200));
      card.innerHTML = '<div class="cp-card-thumb">' + '<div class="cp-text-preview-wrap">' + '<div class="cp-text-blur-top"></div>' + '<span class="cp-text-highlight">' + preview + '</span>' + '<div class="cp-text-blur-bottom"></div>' + '</div>' + '</div>' + '<div class="cp-card-footer">' + '<span class="cp-card-badge">Text</span>' + '<span class="cp-card-name">' + escapeHtml(item.source || 'Document') + '</span>' + '</div>';
      card.addEventListener('click', function () {
        return openTextModal(item);
      });
    }
    card.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        card.click();
      }
    });
    return card;
  };

  /* ============================================================
     VIDEO MODAL
     ============================================================ */
  var openVideoModal = function openVideoModal(item) {
    var videoUrl = '/local/craftpilot/video_proxy.php?id=' + encodeURIComponent(item.id || '');
    var startMs = item.start_time != null ? item.start_time : null;
    var endMs = item.end_time != null ? item.end_time : null;
    var _createModal = createModal(),
      overlay = _createModal.overlay,
      modal = _createModal.modal,
      closeModal = _createModal.closeModal;
    modal.innerHTML = '<div class="cp-modal-header">' + '<h2 class="cp-modal-title">' + escapeHtml(item.filename || item.project_name || 'Video') + '</h2>' + '<button class="cp-icon-btn cp-modal-close" aria-label="Close">' + svgClose() + '</button>' + '</div>' + '<div class="cp-modal-body">' + '<video class="cp-video-player" controls preload="metadata">' + '<source src="' + videoUrl + '" type="video/mp4">' + 'Your browser does not support the video element.' + '</video>' + (startMs !== null ? '<p style="margin:12px 0 0;font-size:13px;color:#767676;font-family:var(--cp-font-body)">' + 'Segment: ' + formatTime(startMs) + ' — ' + formatTime(endMs || 0) + '</p>' : '') + '</div>';
    modal.querySelector('.cp-modal-close').addEventListener('click', closeModal);
    var video = modal.querySelector('video');
    if (startMs !== null) {
      video.addEventListener('loadedmetadata', function () {
        video.currentTime = startMs;
      });
    }
    document.body.appendChild(overlay);
    requestAnimationFrame(function () {
      return overlay.classList.add('cp-modal-overlay--visible');
    });
  };

  /* ============================================================
     BVH MODAL  (Three.js skeleton viewer)
     ============================================================ */
  var openBVHModal = function openBVHModal(item) {
    var rawUrl = item.bvh_url || item.url || '';
    var backendBase = state.chatProxyUrl ? state.chatProxyUrl.replace('/local/craftpilot/chat_proxy.php', '') : '';
    var bvhUrl = rawUrl.startsWith('http') ? rawUrl : backendBase + rawUrl;
    var canvasId = 'cp-bvh-canvas-' + Date.now();
    var _createModal2 = createModal(),
      overlay = _createModal2.overlay,
      modal = _createModal2.modal,
      closeModal = _createModal2.closeModal;
    modal.innerHTML = '<div class="cp-modal-header">' + '<h2 class="cp-modal-title">' + escapeHtml(item.filename || 'Motion Capture') + '</h2>' + '<button class="cp-icon-btn cp-modal-close" aria-label="Close">' + svgClose() + '</button>' + '</div>' + '<div class="cp-modal-body">' + '<div class="cp-bvh-canvas-wrap" id="' + canvasId + '">' + '<div class="cp-bvh-loading">' + '<div class="cp-spinner"></div>' + "<span>Loading motion data\u2026</span>" + '</div>' + '</div>' + '<div class="cp-bvh-controls">' + '<button class="cp-bvh-play" id="cp-bvh-play-btn" aria-label="Play / Pause">' + svgPlay() + '</button>' + '<input type="range" class="cp-bvh-scrubber" min="0" max="1000" value="0" step="1" aria-label="Playback position">' + '<select class="cp-bvh-speed" aria-label="Playback speed">' + "<option value=\"0.25\">0.25\xD7</option>" + "<option value=\"0.5\">0.5\xD7</option>" + "<option value=\"1\" selected>1\xD7</option>" + "<option value=\"2\">2\xD7</option>" + '</select>' + '</div>' + '</div>';
    var doClose = function doClose() {
      var v = window._cpBvhViewer;
      if (v) {
        v.destroy();
        window._cpBvhViewer = null;
      }
      closeModal();
    };
    modal.querySelector('.cp-modal-close').addEventListener('click', doClose);
    document.body.appendChild(overlay);
    requestAnimationFrame(function () {
      return overlay.classList.add('cp-modal-overlay--visible');
    });
    var container = document.getElementById(canvasId);
    var playBtn = modal.querySelector('#cp-bvh-play-btn');
    var scrubber = modal.querySelector('.cp-bvh-scrubber');
    var speedSel = modal.querySelector('.cp-bvh-speed');
    loadThreeJS().then(function () {
      var viewer = new BVHViewer(container, bvhUrl);
      window._cpBvhViewer = viewer;
      return viewer.init();
    }).then(function () {
      var viewer = window._cpBvhViewer;
      playBtn.addEventListener('click', function () {
        viewer.togglePlay();
        playBtn.innerHTML = viewer.playing ? svgPause() : svgPlay();
      });
      viewer.onProgress = function (p) {
        scrubber.value = Math.round(p * 1000);
      };
      scrubber.addEventListener('input', function (e) {
        viewer.seekTo(parseInt(e.target.value, 10) / 1000);
      });
      speedSel.addEventListener('change', function (e) {
        viewer.speed = parseFloat(e.target.value);
      });
    }).catch(function (err) {
      container.innerHTML = '<div class="cp-bvh-loading">' + '<p style="color:#dc2626;font-family:var(--cp-font-body);font-size:13px">' + 'Could not load BVH: ' + escapeHtml(err.message) + '</p>' + '</div>';
    });
  };

  /* ============================================================
     TEXT MODAL  — redirects to the Moodle course module page
     when module info is available; falls back to content preview
     ============================================================ */
  var openTextModal = function openTextModal(item) {
    if (item.module_id && item.module_type) {
      var moduleUrl;
      if (item.module_type === 'label') {
        moduleUrl = '/course/view.php?id=' + encodeURIComponent(item.course_id) + '#module-' + encodeURIComponent(item.module_id);
      } else {
        moduleUrl = '/mod/' + encodeURIComponent(item.module_type) + '/view.php?id=' + encodeURIComponent(item.module_id);
      }
      window.open(moduleUrl, '_blank', 'noopener');
      return;
    }

    /* Fallback: show content in a modal when no module link available */
    var _createModal3 = createModal(),
      overlay = _createModal3.overlay,
      modal = _createModal3.modal,
      closeModal = _createModal3.closeModal;
    modal.innerHTML = '<div class="cp-modal-header">' + '<h2 class="cp-modal-title">' + escapeHtml(item.source || 'Document') + '</h2>' + '<button class="cp-icon-btn cp-modal-close" aria-label="Close">' + svgClose() + '</button>' + '</div>' + '<div class="cp-text-modal-body">' + '<p>' + escapeHtml(item.content || '') + '</p>' + '</div>';
    modal.querySelector('.cp-modal-close').addEventListener('click', closeModal);
    document.body.appendChild(overlay);
    requestAnimationFrame(function () {
      return overlay.classList.add('cp-modal-overlay--visible');
    });
  };

  /* ============================================================
     MODAL UTILITIES
     ============================================================ */
  var createModal = function createModal() {
    var overlay = document.createElement('div');
    overlay.className = 'cp-modal-overlay';
    var modal = document.createElement('div');
    modal.className = 'cp-modal';
    overlay.appendChild(modal);
    var closeModal = function closeModal() {
      overlay.classList.remove('cp-modal-overlay--visible');
      setTimeout(function () {
        if (overlay.parentNode) overlay.parentNode.removeChild(overlay);
      }, 320);
    };
    overlay.addEventListener('click', function (e) {
      if (e.target === overlay) closeModal();
    });
    var _onEsc = function onEsc(e) {
      if (e.key === 'Escape') {
        closeModal();
        document.removeEventListener('keydown', _onEsc);
      }
    };
    document.addEventListener('keydown', _onEsc);
    return {
      overlay: overlay,
      modal: modal,
      closeModal: closeModal
    };
  };

  /* ============================================================
     LAZY-LOAD THREE.JS
     ============================================================ */
  var loadThreeJS = function loadThreeJS() {
    return new Promise(function (resolve, reject) {
      if (window.THREE) {
        resolve();
        return;
      }
      var s = document.createElement('script');
      s.src = 'https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js';
      s.onload = resolve;
      s.onerror = function () {
        return reject(new Error('Failed to load Three.js'));
      };
      document.head.appendChild(s);
    });
  };

  /* ============================================================
     BVH VIEWER  (Three.js WebGL skeleton animator)
     ============================================================ */
  var BVHViewer = /*#__PURE__*/function () {
    function BVHViewer(container, url) {
      _classCallCheck(this, BVHViewer);
      this.container = container;
      this.url = url;
      this.playing = false;
      this.speed = 1;
      this.frameIndex = 0;
      this.frames = [];
      this.frameTime = 1 / 30;
      this.elapsed = 0;
      this.lastTs = 0;
      this.animFrame = null;
      this.onProgress = null;
      this._scene = null;
      this._camera = null;
      this._renderer = null;
      this._root = null;
      this._bonePairs = [];
      this._bonePos = null;
      this._boneLines = null;
      this._spheres = [];
    }
    return _createClass(BVHViewer, [{
      key: "init",
      value: function () {
        var _init = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee() {
          var THREE, w, h, grid, res, text, parsed;
          return _regenerator().w(function (_context) {
            while (1) switch (_context.n) {
              case 0:
                THREE = window.THREE;
                w = Math.max(this.container.clientWidth || 520, 100);
                h = 320;
                this._scene = new THREE.Scene();
                this._scene.background = new THREE.Color(0xfafafa);
                this._camera = new THREE.PerspectiveCamera(50, w / h, 0.1, 50000);
                this._renderer = new THREE.WebGLRenderer({
                  antialias: true
                });
                this._renderer.setSize(w, h);
                this._renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2));
                this.container.innerHTML = '';
                this.container.appendChild(this._renderer.domElement);
                grid = new THREE.GridHelper(1200, 30, 0xe8e8e8, 0xededed);
                this._scene.add(grid);
                this._scene.add(new THREE.AmbientLight(0xffffff, 1));
                _context.n = 1;
                return fetch(this.url);
              case 1:
                res = _context.v;
                if (res.ok) {
                  _context.n = 2;
                  break;
                }
                throw new Error('HTTP ' + res.status + ' fetching BVH');
              case 2:
                _context.n = 3;
                return res.text();
              case 3:
                text = _context.v;
                parsed = parseBVH(text);
                this._root = parsed.root;
                this.frames = parsed.frames;
                this.frameTime = parsed.frameTime;
                if (this.frames.length > 0) {
                  applyFrame(this._root, this.frames[0]);
                }
                this._buildGeometry();
                this._updateGeometry();
                this._autofitCamera();
                this._animate(0);
              case 4:
                return _context.a(2);
            }
          }, _callee, this);
        }));
        function init() {
          return _init.apply(this, arguments);
        }
        return init;
      }()
    }, {
      key: "_buildGeometry",
      value: function _buildGeometry() {
        var _this = this;
        var THREE = window.THREE;
        var _collect = function collect(joint) {
          joint.children.forEach(function (child) {
            _this._bonePairs.push([joint, child]);
            if (!child.isEndSite) _collect(child);
          });
        };
        _collect(this._root);
        var n = this._bonePairs.length;
        this._bonePos = new Float32Array(n * 6);
        var geo = new THREE.BufferGeometry();
        var attr = new THREE.BufferAttribute(this._bonePos, 3);
        attr.setUsage(THREE.DynamicDrawUsage);
        geo.setAttribute('position', attr);
        geo.setDrawRange(0, n * 2);
        var mat = new THREE.LineBasicMaterial({
          color: 0x0f6cbf,
          linewidth: 2
        });
        this._boneLines = new THREE.LineSegments(geo, mat);
        this._scene.add(this._boneLines);
        var sGeo = new THREE.SphereGeometry(3, 6, 6);
        var sMat = new THREE.MeshBasicMaterial({
          color: 0x000000
        });
        var _addSpheres = function addSpheres(joint) {
          if (!joint.isEndSite) {
            var mesh = new THREE.Mesh(sGeo, sMat);
            _this._scene.add(mesh);
            _this._spheres.push({
              joint: joint,
              mesh: mesh
            });
            joint.children.forEach(function (c) {
              return _addSpheres(c);
            });
          }
        };
        _addSpheres(this._root);
      }
    }, {
      key: "_updateGeometry",
      value: function _updateGeometry() {
        var _this2 = this;
        var i = 0;
        this._bonePairs.forEach(function (_ref3) {
          var _ref4 = _slicedToArray(_ref3, 2),
            parent = _ref4[0],
            child = _ref4[1];
          var pp = parent._worldPos;
          var cp = child._worldPos || pp;
          _this2._bonePos[i++] = pp.x;
          _this2._bonePos[i++] = pp.y;
          _this2._bonePos[i++] = pp.z;
          _this2._bonePos[i++] = cp.x;
          _this2._bonePos[i++] = cp.y;
          _this2._bonePos[i++] = cp.z;
        });
        this._boneLines.geometry.attributes.position.needsUpdate = true;
        this._spheres.forEach(function (_ref5) {
          var joint = _ref5.joint,
            mesh = _ref5.mesh;
          if (joint._worldPos) mesh.position.copy(joint._worldPos);
        });
      }
    }, {
      key: "_autofitCamera",
      value: function _autofitCamera() {
        var THREE = window.THREE;
        var box = new THREE.Box3();
        this._spheres.forEach(function (_ref6) {
          var joint = _ref6.joint;
          if (joint._worldPos) box.expandByPoint(joint._worldPos);
        });
        var center = new THREE.Vector3();
        var size = new THREE.Vector3();
        box.getCenter(center);
        box.getSize(size);
        var maxDim = Math.max(size.x, size.y, size.z, 1);
        var dist = maxDim * 2.0;
        this._camera.position.set(center.x, center.y + size.y * 0.05, center.z + dist);
        this._camera.lookAt(center);
        this._camera.far = dist * 12;
        this._camera.updateProjectionMatrix();
      }
    }, {
      key: "_animate",
      value: function _animate(ts) {
        var _this3 = this;
        this.animFrame = requestAnimationFrame(function (t) {
          return _this3._animate(t);
        });
        if (this.playing && this.frames.length > 0) {
          var delta = (ts - this.lastTs) / 1000;
          this.elapsed += delta * this.speed;
          var total = this.frames.length * this.frameTime;
          if (this.elapsed >= total) this.elapsed = this.elapsed % total;
          this.frameIndex = Math.min(Math.floor(this.elapsed / this.frameTime), this.frames.length - 1);
          applyFrame(this._root, this.frames[this.frameIndex]);
          this._updateGeometry();
          if (this.onProgress) this.onProgress(this.elapsed / total);
        }
        this.lastTs = ts;
        this._renderer.render(this._scene, this._camera);
      }
    }, {
      key: "togglePlay",
      value: function togglePlay() {
        this.playing = !this.playing;
      }
    }, {
      key: "seekTo",
      value: function seekTo(progress) {
        var total = this.frames.length * this.frameTime;
        this.elapsed = progress * total;
        this.frameIndex = Math.min(Math.floor(this.elapsed / this.frameTime), this.frames.length - 1);
        applyFrame(this._root, this.frames[this.frameIndex]);
        this._updateGeometry();
        if (this._renderer) this._renderer.render(this._scene, this._camera);
      }
    }, {
      key: "destroy",
      value: function destroy() {
        if (this.animFrame) {
          cancelAnimationFrame(this.animFrame);
          this.animFrame = null;
        }
        if (this._renderer) {
          this._renderer.dispose();
          this._renderer = null;
        }
        if (this._boneLines) {
          this._boneLines.geometry.dispose();
          this._boneLines.material.dispose();
        }
      }
    }]);
  }();
  /* ============================================================
     BVH PARSER
     ============================================================ */
  var parseBVH = function parseBVH(text) {
    var lines = text.split(/\r?\n/);
    var cursor = 0;
    var read = function read() {
      while (cursor < lines.length) {
        var l = lines[cursor++].trim();
        if (l) return l;
      }
      return null;
    };
    var line;
    while ((line = read()) && line !== 'HIERARCHY') {/* skip */}
    var allJoints = [];
    var _parseJoint = function parseJoint(parent) {
      var decl = read();
      var parts = decl.split(/\s+/);
      var isEnd = parts[0] === 'End';
      var name = isEnd ? (parent ? parent.name : '') + '_end' : parts[1];
      var joint = {
        name: name,
        parent: parent,
        offset: [0, 0, 0],
        channels: [],
        children: [],
        isEndSite: isEnd,
        _worldPos: null
      };
      if (!isEnd) allJoints.push(joint);
      read(); /* opening { */

      var l;
      while ((l = read()) !== '}') {
        var p = l.split(/\s+/);
        if (p[0] === 'OFFSET') {
          joint.offset = [parseFloat(p[1]), parseFloat(p[2]), parseFloat(p[3])];
        } else if (p[0] === 'CHANNELS') {
          var n = parseInt(p[1], 10);
          for (var i = 0; i < n; i++) joint.channels.push(p[2 + i]);
        } else if (p[0] === 'JOINT') {
          cursor--;
          var child = _parseJoint(joint);
          joint.children.push(child);
        } else if (p[0] === 'End') {
          cursor--;
          var endSite = _parseJoint(joint);
          joint.children.push(endSite);
        }
      }
      return joint;
    };
    var root = _parseJoint(null);
    while ((line = read()) && line !== 'MOTION') {/* skip */}
    var framesLine = read();
    var numFrames = parseInt((framesLine || '').replace(/[^0-9]/g, ''), 10) || 0;
    var ftLine = read();
    var ftParts = (ftLine || '').split(':');
    var frameTime = parseFloat(ftParts[1] != null ? ftParts[1] : ftParts[0]) || 1 / 30;
    var frames = [];
    for (var i = 0; i < numFrames; i++) {
      var fl = read();
      if (!fl) break;
      frames.push(fl.trim().split(/\s+/).map(parseFloat));
    }
    return {
      root: root,
      joints: allJoints,
      frames: frames,
      frameTime: frameTime
    };
  };

  /* ============================================================
     BVH FORWARD KINEMATICS
     ============================================================ */
  var DEG2RAD = Math.PI / 180;
  var applyFrame = function applyFrame(root, frame) {
    if (!frame || !window.THREE) return;
    var THREE = window.THREE;
    var ref = {
      idx: 0
    };
    var _traverse = function traverse(joint, parentMat) {
      var tx = joint.offset[0];
      var ty = joint.offset[1];
      var tz = joint.offset[2];
      var rx = 0,
        ry = 0,
        rz = 0;
      var rotAxes = [];
      if (!joint.isEndSite) {
        joint.channels.forEach(function (ch) {
          var val = frame[ref.idx] !== undefined ? frame[ref.idx] : 0;
          ref.idx++;
          switch (ch) {
            case 'Xposition':
              tx += val;
              break;
            case 'Yposition':
              ty += val;
              break;
            case 'Zposition':
              tz += val;
              break;
            case 'Xrotation':
              rx = val * DEG2RAD;
              rotAxes.push('X');
              break;
            case 'Yrotation':
              ry = val * DEG2RAD;
              rotAxes.push('Y');
              break;
            case 'Zrotation':
              rz = val * DEG2RAD;
              rotAxes.push('Z');
              break;
          }
        });
      }
      var order = rotAxes.join('') || 'ZXY';
      var T = new THREE.Matrix4().makeTranslation(tx, ty, tz);
      var R = new THREE.Matrix4().makeRotationFromEuler(new THREE.Euler(rx, ry, rz, order));
      var localMat = new THREE.Matrix4().multiplyMatrices(T, R);
      var worldMat = parentMat ? new THREE.Matrix4().multiplyMatrices(parentMat, localMat) : localMat.clone();
      joint._worldPos = new THREE.Vector3().setFromMatrixPosition(worldMat);
      joint.children.forEach(function (c) {
        return _traverse(c, worldMat);
      });
    };
    _traverse(root, null);
  };

  /* ============================================================
     CONVERSATIONS
     ============================================================ */
  var loadConversations = function loadConversations() {
    Ajax.call([{
      methodname: 'local_craftpilot_manage_conversations',
      args: {
        action: 'list',
        courseid: state.courseId || 0
      }
    }])[0].then(function (resp) {
      var convs = resp.conversations || resp.data || [];
      if (resp.success && convs.length > 0) {
        state.conversations = convs;
        var first = convs[0];
        selectConversation(first.conversation_id || first.id, first.title || 'Chat');
      } else {
        createConversation(generateUUID());
      }
    }).catch(function (err) {
      console.error('CraftPilot:', err);
      showError('Failed to initialise chat. Please refresh the page.');
    });
  };
  var createConversation = function createConversation(id) {
    // Clear UI immediately (sync) — prevents race that shows old conv content
    state.currentConvId = id;
    state.currentConvTitle = 'New conversation';
    state.isNewConv = true;
    state.conversations.unshift({
      conversation_id: id,
      title: 'New conversation',
      created_time: Math.floor(Date.now() / 1000)
    });
    dom.messages.innerHTML = '';
    getConvState(id).sources = [];
    clearSources();
    renderConversations(state.conversations, id);
    updatePanelTitle(state.currentConvTitle);
    showReady();
    // Persist to backend — fire-and-forget
    Ajax.call([{
      methodname: 'local_craftpilot_manage_conversations',
      args: {
        action: 'create',
        conversation_id: id,
        title: 'New conversation',
        courseid: state.courseId || 0,
        metadata: JSON.stringify({
          created: new Date().toISOString()
        })
      }
    }])[0].catch(function (err) {
      return console.error('CraftPilot:', err);
    });
  };
  var startNewConversation = function startNewConversation() {
    return createConversation(generateUUID());
  };
  var selectConversation = function selectConversation(id, title) {
    // Save sources for the conversation we're leaving
    if (state.currentConvId) {
      getConvState(state.currentConvId).sources = _toConsumableArray(state.sources);
    }
    state.currentConvId = id;
    state.currentConvTitle = title;
    updatePanelTitle(title);
    renderConversations(state.conversations, id);
    dom.messages.innerHTML = '';
    // Restore saved sources (or clear if none for this conv)
    setSources(getConvState(id).sources);
    loadMessages(id);
  };
  var deleteConversation = function deleteConversation(id) {
    if (!window.confirm('Delete this conversation and all its messages?')) return;
    Ajax.call([{
      methodname: 'local_craftpilot_manage_conversations',
      args: {
        action: 'delete',
        conversation_id: id
      }
    }])[0].then(function (resp) {
      if (!resp.success) return;
      delete convStates[String(id)];
      state.conversations = state.conversations.filter(function (c) {
        return String(c.conversation_id || c.id) !== String(id);
      });
      if (String(state.currentConvId) === String(id)) {
        dom.messages.innerHTML = '';
        clearSources();
        if (state.conversations.length > 0) {
          var next = state.conversations[0];
          selectConversation(next.conversation_id || next.id, next.title || 'Chat');
        } else {
          createConversation(generateUUID());
        }
      } else {
        renderConversations(state.conversations, state.currentConvId);
      }
    }).catch(function (err) {
      return console.error('CraftPilot:', err);
    });
  };

  /* ============================================================
     CONVERSATION LIST RENDER
     ============================================================ */
  var renderConversations = function renderConversations(convs, activeId) {
    if (!dom.convList) return;
    dom.convList.innerHTML = '';
    convs.forEach(function (conv) {
      var id = String(conv.conversation_id || conv.id);
      var item = document.createElement('div');
      item.className = 'cp-conv-item' + (id === String(activeId) ? ' cp-conv-item--active' : '');
      item.setAttribute('role', 'listitem');
      item.dataset.id = id;
      var ts = conv.created_time || 0;
      var ms = ts > 1e10 ? ts : ts * 1000;
      var date = new Date(ms).toLocaleDateString();
      item.innerHTML = '<div class="cp-conv-info">' + '<span class="cp-conv-title">' + escapeHtml(conv.title || 'Chat') + '</span>' + '<span class="cp-conv-date">' + date + '</span>' + '</div>' + '<button class="cp-conv-delete" aria-label="Delete conversation" title="Delete">' + '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">' + '<polyline points="3 6 5 6 21 6"/>' + '<path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>' + '</svg>' + '</button>';
      item.addEventListener('click', function (e) {
        if (e.target.closest('.cp-conv-delete')) {
          e.stopPropagation();
          deleteConversation(id);
        } else {
          selectConversation(id, conv.title || 'Chat');
        }
      });
      dom.convList.appendChild(item);
    });
  };

  /* ============================================================
     MESSAGES
     ============================================================ */
  var loadMessages = function loadMessages(convId) {
    Ajax.call([{
      methodname: 'local_craftpilot_manage_messages',
      args: {
        action: 'load',
        conversation_id: convId
      }
    }])[0].then(function (resp) {
      if (String(convId) !== String(state.currentConvId)) return; // stale response
      var msgs = resp.messages || resp.data || [];
      if (resp.success && msgs.length > 0) {
        dom.messages.innerHTML = '';
        msgs.forEach(function (m) {
          return appendMessage(m.message_type === 'user' || m.role === 'user' ? 'user' : 'ai', m.content, false);
        });
        scrollBottom();
        // Restore sources from the last AI message's metadata (only if none already in memory)
        if (getConvState(convId).sources.length === 0) {
          var lastAi = _toConsumableArray(msgs).reverse().find(function (m) {
            return m.message_type === 'ai' || m.role === 'ai';
          });
          if (lastAi && lastAi.metadata) {
            try {
              var meta = JSON.parse(lastAi.metadata);
              if (Array.isArray(meta.sources) && meta.sources.length > 0) {
                getConvState(convId).sources = meta.sources;
                setSources(meta.sources);
              }
            } catch (_) {/* malformed metadata — skip */}
          }
        }
      } else {
        showReady();
      }
    }).catch(function (err) {
      return console.error('CraftPilot:', err);
    });
  };
  var appendMessage = function appendMessage(role, content, animate) {
    hideEmpty();
    var wrapper = document.createElement('div');
    wrapper.className = 'cp-msg cp-msg--' + (role === 'user' ? 'user' : 'ai');
    if (animate === false) wrapper.style.animation = 'none';
    var bubble = document.createElement('div');
    bubble.className = 'cp-msg-bubble';
    if (role === 'user') {
      bubble.textContent = content;
    } else {
      var clean = stripThinkTags(content);
      bubble.innerHTML = renderMarkdown(clean);
      extractFollowUpQuestions(clean);
    }
    wrapper.appendChild(bubble);
    dom.messages.appendChild(wrapper);
    return wrapper;
  };
  var showTyping = function showTyping() {
    hideEmpty();
    var wrapper = document.createElement('div');
    wrapper.className = 'cp-msg cp-msg--ai';
    wrapper.id = 'cp-typing-indicator';
    wrapper.innerHTML = '<div class="cp-typing">' + '<div class="cp-typing-dot"></div>' + '<div class="cp-typing-dot"></div>' + '<div class="cp-typing-dot"></div>' + '<span class="cp-status-hint" aria-live="polite"></span>' + '</div>';
    dom.messages.appendChild(wrapper);
    scrollBottom();
    return wrapper;
  };
  var hideEmpty = function hideEmpty() {
    var el = document.getElementById('cp-empty-state');
    if (el) el.parentNode.removeChild(el);
  };
  var showReady = function showReady() {
    if (document.getElementById('cp-empty-state')) return;
    var el = document.createElement('div');
    el.className = 'cp-empty-state';
    el.id = 'cp-empty-state';
    el.innerHTML = '<svg class="cp-empty-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">' + '<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>' + '</svg>' + '<p class="cp-empty-title">Ready to assist</p>' + '<p class="cp-empty-sub">Ask a question about this content</p>';
    dom.messages.appendChild(el);
  };
  var showError = function showError(msg) {
    var el = appendMessage('ai', msg, true);
    el.querySelector('.cp-msg-bubble').style.color = '#dc2626';
  };

  /* ============================================================
     SEND MESSAGE + STREAM
     ============================================================ */
  var sendMessage = function sendMessage() {
    var text = dom.input.value.trim();
    if (!text || state.streaming) return;
    if (!state.currentConvId) {
      showError('Chat not ready yet — please wait a moment.');
      return;
    }
    if (/^[ABC]$/i.test(text)) {
      var letter = text.toUpperCase();
      var expanded = state.followUpQuestions[letter];
      if (expanded) text = expanded;
    }
    state.streaming = true;
    dom.input.disabled = true;
    dom.sendBtn.disabled = true;
    appendMessage('user', text, true);
    dom.input.value = '';
    autoResize();
    getConvState(state.currentConvId).sources = [];
    clearSources();
    scrollBottom();
    var isFirst = state.isNewConv;
    state.isNewConv = false;
    saveMessage(state.currentConvId, 'user', text);
    streamFromBackend(text, isFirst);
  };
  var streamFromBackend = function streamFromBackend(userMessage) {
    var isFirstMessage = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    var streamConvId = state.currentConvId; // capture now — don't read live inside stream
    var typingEl = showTyping();
    var controller = new AbortController();
    state.streamAbort = controller;
    var payload = {
      message: userMessage,
      conversation_thread_id: state.currentConvId,
      is_first_message: isFirstMessage,
      sesskey: window.M && window.M.cfg ? window.M.cfg.sesskey : '',
      user_id: window.M && window.M.cfg ? window.M.cfg.userid : 0
    };
    if (state.selectedDomain) {
      payload.selected_domain = state.selectedDomain;
    }
    if (state.courseId) {
      payload.course_id = String(state.courseId);
    }
    Promise.resolve(fetch(state.chatProxyUrl, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(payload),
      signal: controller.signal
    })).then(function (res) {
      if (String(streamConvId) !== String(state.currentConvId)) {
        res.body.cancel();
        finishStreaming();
        return;
      }
      if (!res.ok) throw new Error('Backend responded ' + res.status);

      // Typing indicator stays alive while status events cycle through.
      // msgEl/bubble are created lazily on the first token so the hint
      // remains visible during the full retrieval + reranking phase.
      var msgEl = null;
      var bubble = null;
      var fullResponse = '';
      var activateBubble = function activateBubble() {
        if (msgEl) return bubble;
        if (typingEl.parentNode) typingEl.parentNode.removeChild(typingEl);
        msgEl = appendMessage('ai', '', true);
        bubble = msgEl.querySelector('.cp-msg-bubble');
        return bubble;
      };
      var reader = res.body.getReader();
      var decoder = new TextDecoder();
      var buf = '';
      var _read = /*#__PURE__*/function () {
        var _ref7 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2() {
          var _yield$reader$read, done, value, b, cleanFinal, finishedSources, lines, _iterator, _step, line, ev, newTitle, conv, hintEl, vm, isBVH, bm, visible, _visible, _t, _t2;
          return _regenerator().w(function (_context2) {
            while (1) switch (_context2.p = _context2.n) {
              case 0:
                _context2.n = 1;
                return reader.read();
              case 1:
                _yield$reader$read = _context2.v;
                done = _yield$reader$read.done;
                value = _yield$reader$read.value;
                if (!done) {
                  _context2.n = 3;
                  break;
                }
                if (!(String(streamConvId) !== String(state.currentConvId))) {
                  _context2.n = 2;
                  break;
                }
                finishStreaming();
                return _context2.a(2);
              case 2:
                b = activateBubble();
                cleanFinal = stripThinkTags(fullResponse);
                b.innerHTML = renderMarkdown(cleanFinal);
                extractFollowUpQuestions(cleanFinal);
                finishedSources = getConvState(streamConvId).sources;
                saveMessage(streamConvId, 'ai', cleanFinal, finishedSources.length ? JSON.stringify({
                  sources: finishedSources
                }) : '');
                finishStreaming();
                return _context2.a(2);
              case 3:
                buf += decoder.decode(value, {
                  stream: true
                });
                lines = buf.split('\n');
                buf = lines.pop();
                _iterator = _createForOfIteratorHelper(lines);
                _context2.p = 4;
                _iterator.s();
              case 5:
                if ((_step = _iterator.n()).done) {
                  _context2.n = 13;
                  break;
                }
                line = _step.value;
                if (line.trim()) {
                  _context2.n = 6;
                  break;
                }
                return _context2.a(3, 12);
              case 6:
                _context2.p = 6;
                ev = JSON.parse(line);
                if (!(ev.event === 'conversation_title' && ev.data)) {
                  _context2.n = 7;
                  break;
                }
                newTitle = ev.data;
                state.currentConvTitle = newTitle;
                updatePanelTitle(newTitle);
                conv = state.conversations.find(function (c) {
                  return String(c.conversation_id || c.id) === String(streamConvId);
                });
                if (conv) conv.title = newTitle;
                renderConversations(state.conversations, streamConvId);
                Ajax.call([{
                  methodname: 'local_craftpilot_manage_conversations',
                  args: {
                    action: 'update',
                    conversation_id: streamConvId,
                    title: newTitle
                  }
                }])[0].catch(function (err) {
                  return console.error('CraftPilot: title persist failed', err);
                });
                _context2.n = 10;
                break;
              case 7:
                if (!(ev.event === 'status' && ev.data)) {
                  _context2.n = 9;
                  break;
                }
                hintEl = typingEl.querySelector('.cp-status-hint');
                if (hintEl) hintEl.textContent = ev.data;
                // Yield one animation frame so the browser renders this
                // hint before processing the next event in the same chunk.
                _context2.n = 8;
                return new Promise(function (resolve) {
                  return requestAnimationFrame(resolve);
                });
              case 8:
                _context2.n = 10;
                break;
              case 9:
                if (ev.event === 'video_metadata' && ev.data) {
                  vm = ev.data;
                  isBVH = (vm.filename || '').toLowerCase().endsWith('.bvh');
                  addSource({
                    id: vm.video_id || vm.bvh_id || vm.filename,
                    type: isBVH ? 'bvh' : 'video',
                    filename: vm.filename,
                    video_url: isBVH ? null : vm.video_url,
                    bvh_url: isBVH ? vm.bvh_url || vm.video_url : null,
                    start_time: vm.start_time,
                    end_time: vm.end_time,
                    duration: vm.duration,
                    project_name: vm.project_name
                  }, streamConvId);
                } else if (ev.event === 'bvh_metadata' && ev.data) {
                  bm = ev.data;
                  addSource({
                    id: bm.bvh_id || bm.filename,
                    type: 'bvh',
                    filename: bm.filename,
                    bvh_url: bm.bvh_url || bm.url,
                    duration: bm.duration,
                    frame_count: bm.frame_count
                  }, streamConvId);
                } else if (ev.event === 'token' && ev.data) {
                  fullResponse += ev.data;
                  visible = stripThinkTags(fullResponse);
                  activateBubble().innerHTML = renderMarkdown(visible);
                  scrollBottom();
                } else if (ev.event === 'message' && ev.content) {
                  ev.content.forEach(function (c) {
                    if (c.content) fullResponse += c.content;
                  });
                  _visible = stripThinkTags(fullResponse);
                  activateBubble().innerHTML = renderMarkdown(_visible);
                  scrollBottom();
                } else if (ev.event === 'documents' && Array.isArray(ev.data)) {
                  ev.data.forEach(function (doc) {
                    if (doc.type === 'video_annotation') return;
                    addSource({
                      id: doc.source,
                      type: 'text',
                      source: doc.module_name || doc.source,
                      content: doc.page_content_preview,
                      module_id: doc.module_id,
                      module_type: doc.module_type,
                      course_id: doc.course_id,
                      heading_path: doc.heading_path,
                      section_name: doc.section_name
                    }, streamConvId);
                  });
                } else if (ev.content === '[DONE]') {
                  /* handled on stream close */
                } else if (ev.event === 'error' || ev.type === 'error') {
                  if (msgEl && msgEl.parentNode) msgEl.parentNode.removeChild(msgEl);
                  if (typingEl.parentNode) typingEl.parentNode.removeChild(typingEl);
                  showError(ev.message || 'The AI backend returned an error.');
                  finishStreaming();
                }
              case 10:
                _context2.n = 12;
                break;
              case 11:
                _context2.p = 11;
                _t = _context2.v;
              case 12:
                _context2.n = 5;
                break;
              case 13:
                _context2.n = 15;
                break;
              case 14:
                _context2.p = 14;
                _t2 = _context2.v;
                _iterator.e(_t2);
              case 15:
                _context2.p = 15;
                _iterator.f();
                return _context2.f(15);
              case 16:
                return _context2.a(2, _read());
            }
          }, _callee2, null, [[6, 11], [4, 14, 15, 16]]);
        }));
        return function read() {
          return _ref7.apply(this, arguments);
        };
      }();
      return _read();
    }).catch(function (err) {
      if (err.name === 'AbortError') {
        if (typingEl.parentNode) typingEl.parentNode.removeChild(typingEl);
        finishStreaming();
        return;
      }
      console.error('CraftPilot:', err);
      if (typingEl.parentNode) typingEl.parentNode.removeChild(typingEl);
      showError('Erreur: ' + err.name + ' — ' + err.message);
      finishStreaming();
    });
  };
  var finishStreaming = function finishStreaming() {
    var _dom$input2;
    state.streaming = false;
    state.streamAbort = null;
    dom.input.disabled = false;
    dom.sendBtn.disabled = false;
    (_dom$input2 = dom.input) === null || _dom$input2 === void 0 || _dom$input2.focus();
  };

  /* ============================================================
     SAVE MESSAGE  (fire-and-forget)
     ============================================================ */
  var saveMessage = function saveMessage(convId, role, content) {
    var metadata = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : '';
    Ajax.call([{
      methodname: 'local_craftpilot_manage_messages',
      args: {
        action: 'save',
        conversation_id: convId,
        message_type: role === 'ai' ? 'ai' : 'user',
        content: content,
        metadata: metadata
      }
    }])[0].catch(function (err) {
      return console.error('CraftPilot save error:', err);
    });
  };

  /* ============================================================
     UI HELPERS
     ============================================================ */
  var updatePanelTitle = function updatePanelTitle(title) {
    if (dom.convTitle) dom.convTitle.textContent = title || 'CraftPilot';
  };
  var scrollBottom = function scrollBottom() {
    var el = dom.messages;
    if (!el) return;
    // Only auto-scroll if the user is within 120px of the bottom.
    // This lets the user scroll up to read earlier content without being
    // dragged back down on every token.
    if (el.scrollHeight - el.scrollTop - el.clientHeight < 120) {
      el.scrollTop = el.scrollHeight;
    }
  };
  var autoResize = function autoResize() {
    var el = dom.input;
    if (!el) return;
    el.style.height = 'auto';
    el.style.height = Math.min(el.scrollHeight, 120) + 'px';
  };
  var generateUUID = function generateUUID() {
    if (typeof crypto !== 'undefined' && crypto.randomUUID) return crypto.randomUUID();
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
      var r = Math.random() * 16 | 0;
      return (c === 'x' ? r : r & 0x3 | 0x8).toString(16);
    });
  };
  var escapeHtml = function escapeHtml(s) {
    if (typeof s !== 'string') return '';
    var d = document.createElement('div');
    d.textContent = s;
    return d.innerHTML;
  };
  var stripThinkTags = function stripThinkTags(s) {
    if (typeof s !== 'string') return s;
    return s.replace(/<think>[\s\S]*?<\/think>/gi, '').trim();
  };
  var renderMarkdown = function renderMarkdown(text) {
    var markedFn = MarkedLib && MarkedLib.marked ? MarkedLib.marked : window.marked;
    if (!markedFn) return escapeHtml(text);
    if (!window._cpMarkedConfigured) {
      markedFn.setOptions({
        gfm: true,
        breaks: true
      });
      window._cpMarkedConfigured = true;
    }
    var raw = markedFn.parse(text);
    return _dompurify.default ? _dompurify.default.sanitize(raw) : raw;
  };
  var extractFollowUpQuestions = function extractFollowUpQuestions(text) {
    state.followUpQuestions = {
      A: null,
      B: null,
      C: null
    };
    var pattern = /\*\*([ABC])\.\*\*\s+(.+)/g;
    var m;
    while ((m = pattern.exec(text)) !== null) {
      state.followUpQuestions[m[1]] = m[2].trim();
    }
  };
  var formatTime = function formatTime(secs) {
    var h = Math.floor(secs / 3600);
    var m = Math.floor(secs % 3600 / 60);
    var s = Math.floor(secs % 60);
    return [h, m, s].map(function (n) {
      return String(n).padStart(2, '0');
    }).join(':');
  };
  var svgClose = function svgClose() {
    return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">' + '<line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>' + '</svg>';
  };
  var svgPlay = function svgPlay() {
    return '<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>';
  };
  var svgPause = function svgPause() {
    return '<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">' + '<rect x="6" y="4" width="4" height="16"/><rect x="14" y="4" width="4" height="16"/>' + '</svg>';
  };
});
//# sourceMappingURL=chat_interface.min.js.map
